{
  "name": "taskvra",
  "private": true,
  "version": "1.0.0",
  "description": "Taskvra monorepo deploy entry for Render",
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend install",
    "postinstall": "npm --prefix backend install"
  },
  "engines": {
    "node": ">=18"
  }
}
